build/
*.o
*.a
*.so
*.elf
*.bin
*.iso
*~
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Track C: keep the "ld.so" *directory* (the *.so rule above would
# ignore it wholesale because its name ends in .so)
!/userland/ld.so/
!/userland/ld.so/*
